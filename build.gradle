plugins {
    id "jacoco"
    id "org.sonarqube" version "2.7"
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'org.sonarqube'

    sonarqube {
        properties {
            property 'sonar.projectBaseDir', '.'
            skipProject = false
        }
    }
}